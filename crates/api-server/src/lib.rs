//! AI Wallet — API-сервер (Axum + Tokio).
//!
//! Скелет бекенду за розділом 5 ТЗ. Усі ендпоінти під префіксом `/v1`.
//!
//! # ПРАВИЛО БЕЗПЕКИ (ТЗ розділ 6, п.3)
//!
//! Бекенд **не має** ендпоінтів, що приймають приватні ключі, seed-фрази
//! чи мнемоніки. Жодне DTO-поле не може називатися `private_key`,
//! `secret_key`, `seed`, `seed_phrase`, `mnemonic` тощо. Бекенд оперує
//! ВИКЛЮЧНО публічними даними: адреси, unsigned/signed tx (signed tx — це
//! вже підписаний байткод, він не розкриває ключ).
//!
//! Це правило перевіряється автоматично тестом `tests/security.rs`,
//! який сканує вихідний код на заборонені імена полів.

pub mod ai;
pub mod config;
pub mod dto;
pub mod handlers;
pub mod risk;
pub mod routes;
pub mod state;
